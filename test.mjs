/** #### Runs all tests
 *
 * @example
 * npm test
 */

import { deepEqual as eq } from "node:assert";

// eq(1, 2, "1 is not equal to 2");
eq(1, 1, "1 is equal to 1");

console.log('\n\u2705 All tests passed\n');
